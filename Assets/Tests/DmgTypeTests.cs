namespace Tests
{
    /*
        public class DmgTypeTests
        {
            [Test]
            public void GetAttribute_ShouldReturnCorrectAttribute()
            {
                // Arrange
                const DmgType dmgType = DmgType.Fire;

                // Act
                var attribute = dmgType.GetAttribute();

                // Assert
                Assert.IsNotNull(attribute);
                Assert.AreEqual(DmgCategory.Elemental, attribute.Category);
            }

            [Test]
            public void GetAttribute_ShouldReturnNullForInvalidDmgType()
            {
                // Arrange
                const DmgType invalidDmgType = (DmgType)999;

                // Act
                var attribute = invalidDmgType.GetAttribute();

                // Assert
                Assert.IsNull(attribute);
            }
        }*/
}